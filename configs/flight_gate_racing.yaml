# Native viewer deployment for a trained reference-free gate-racing policy.
task: gate_racing
scene: gate_racing
planner: none
controller: rl

wind: none
# Match the MLP replay: follow the vehicle from behind.
follow_camera: true
seed: 6

rl:
  checkpoint: ../runs/gate_racing/mlp_random_v3/best_model.zip
  device: cpu
